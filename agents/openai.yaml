interface:
  display_name: "优秀工作报告评选"
  short_description: "按日报和周报规则诊断、预判变化、稳健修改并评分评选"
  default_prompt: "Use $work-report-selector to diagnose this report, forecast likely rule changes, and produce a robust fact-preserving revision."
policy:
  allow_implicit_invocation: true
